from unittest.mock import patch

from ckan_cloud_operator.routers import cli
from tests.bases import BaseCliTestCase


class RoutersCliTestCase(BaseCliTestCase):
    #@patch('ckan_cloud_operator.providers.routers.manager.initialize')
    #def test_initialize(self, manager_initialize):
    #    result = self.runner.invoke(cli.initialize)
    #    manager_initialize.assert_called_once_with(False)
    pass
